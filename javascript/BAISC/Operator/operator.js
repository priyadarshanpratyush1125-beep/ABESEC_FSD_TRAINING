let a=5/2;
console.log(a); // It return 2.5  but in c++ it return 2
// == (Loose comparison ) vs === (strict comparison)
let b=5;
let c=5;
let d="5";
console.log(typeof(b));
console.log(typeof(c));

console.log(b==c);  // it will return true as it compare two integer and which is same
console.log(b==d); //it will return true as "5" conert into int 5 

console.log(b===d); // it will retuen false as it compare int and string which is not same 


